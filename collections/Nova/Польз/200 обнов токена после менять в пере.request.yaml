$kind: http-request
name: 200 обнов токена после менять в переменной
url: "{{base_url}}/api/v1/auth/refresh"
method: POST
body:
  type: json
  content: |-
    {
      "refresh_token": "{{refresh_token}}"
    }
order: 4501795276373617
